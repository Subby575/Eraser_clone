/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode:false,
    images: {
        domains: ["lh3.googleusercontent.com","assets.aceternity.com","api.microlink.io","images.unsplash.com"],
    }
};

export default nextConfig;
